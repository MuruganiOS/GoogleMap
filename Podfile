# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GMS_API' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
   pod 'GoogleMaps'
   pod 'GooglePlaces'
  # Pods for GMS_API

  target 'GMS_APITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GMS_APIUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
